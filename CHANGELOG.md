# Changelog

## [0.1.0] - 2026-02-23

### Added

- CLI `repo_scorecard` para puntuar repos locales (checks ponderados 0-100).
- Workflow GitHub Actions para scorecard y CI (tests en paralelo).
- App de prueba `app_prueba`: módulos `calc`, `store`.
- Tests unitarios en `tests/unit/`.
- Tests de integración en `tests/integration/`.
- README, LICENSE, CODEOWNERS, dependabot, Dockerfile, pyproject con ruff y opentelemetry opcional.
